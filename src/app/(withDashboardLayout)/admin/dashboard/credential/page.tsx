import React from "react";
import CreateCredentialsPage from "./_components/CredentialPage/CreateCredentialsPage";
export const dynamic = "force-dynamic";

const Credential = () => {
  return (
    <div>
      <CreateCredentialsPage />
    </div>
  );
};

export default Credential;
